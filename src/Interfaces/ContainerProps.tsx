import * as React from "react";

interface ContainerProps {
  children: React.ReactNode;
  className?: string;
  center?: boolean; // Optional: center content vertically & horizontally
}

export type { ContainerProps };
